

<%= form_for :user, :method => :put do |f| %>

  <%= f.radio_button:_status, :is_moderator, user.is_moderator %>

  <%= f.submit 'Update admin status'%>

<%end%>

<!-- I might have to make an if statement around the form and make 2 forms-->